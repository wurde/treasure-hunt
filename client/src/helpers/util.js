import map from './map'
const wait = ms => {
  return new Promise((res, rej) => setTimeout(res, ms));
};

const pickItem = (object, encumberance) => {

}

const triggerWiseExplorer => ()
export { wait };
